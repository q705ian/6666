# 羊城印记 - 工程核心代码逻辑解析与优化建议

## 一、项目概述

**羊城印记**是一款基于广州旅游场景的跨平台打卡应用，采用 `Expo (React Native)` + `Express.js` 的 monorepo 架构。

| 维度 | 说明 |
|------|------|
| 前端 | Expo SDK 54 + React Native 0.81 + Expo Router 6 |
| 后端 | Express.js 4 + TypeScript |
| 样式 | TailwindCSS v4 + Uniwind（CSS 变量运行时绑定）|
| 包管理 | pnpm workspace |
| 状态管理 | React useState（当前无全局状态库）|

---

## 二、工程架构

```
├── client/                     # Expo 前端
│   ├── app/                    # Expo Router 路由（仅配置）
│   │   ├── _layout.tsx         # 根布局：Stack 导航 + Provider
│   │   ├── (tabs)/             # 底部 Tab 导航
│   │   │   ├── _layout.tsx     # Tab 配置（首页/打卡/成就/我的）
│   │   │   ├── index.tsx       # 首页 → screens/home
│   │   │   ├── checkin.tsx     # 打卡 → screens/checkin
│   │   │   ├── badges.tsx      # 成就 → screens/badges
│   │   │   └── profile.tsx     # 我的 → screens/profile
│   │   ├── chat.tsx            # AI 导游（Stack）
│   │   ├── attraction-detail.tsx # 景点详情（Stack）
│   │   └── checkin-action.tsx  # 打卡动作页（Stack）
│   ├── screens/                # 页面实现
│   ├── components/             # 可复用组件
│   │   ├── Screen.tsx          # 页面容器（安全区 + 键盘避让）
│   │   ├── Provider.tsx        # 全局 Provider 组合
│   │   └── ColorSchemeUpdater.tsx # 主题切换
│   ├── hooks/
│   │   └── useSafeRouter.ts    # 安全路由（Base64 Payload）
│   └── global.css              # TailwindCSS 主题变量
├── server/
│   └── src/
│       └── index.ts            # Express 入口（API + 预设数据）
```

---

## 三、核心功能实现解析

### 3.1 安全路由（useSafeRouter）

**问题**：`expo-router` 的 `push` 不会自动编码参数，而 `useLocalSearchParams` 会自动解码，导致 `%`、`&`、中文等字符传参时损坏；URL 参数全为字符串，类型丢失。

**实现方案**：
- 采用 **Payload 模式**，将所有参数 `JSON.stringify` 后 `Base64` 编码，统一放在 `__safeRouterPayload__` 键下传递。
- 接收端解码还原，兼容嵌套对象、数组、特殊字符，保留原始类型。

```ts
// 编码
const serializeParams = (params) => Base64.encode(JSON.stringify(params));

// 解码
const deserializeParams = (payload) => JSON.parse(Base64.decode(payload));

// 使用
router.push('/detail', { id: 123, uri: 'file:///path/%40test.mp3' });
const { id, uri } = useSafeSearchParams<{ id: number; uri: string }>();
```

### 3.2 页面容器（Screen）

**核心设计**：不使用 `SafeAreaView`，统一用 `View + padding` 手动管理安全区；智能识别子树滚动容器：

1. 自动检测子树是否包含垂直 `ScrollView/FlatList/SectionList`（不递归 `Modal`）。
2. 若页面自身无滚动容器，外层自动包裹 `KeyboardAwareScrollView`。
3. 若页面自带滚动容器，仅给该滚动容器注入键盘避让逻辑，**保证底部固定栏不随键盘上移**。

```ts
// 自动检测
const childIsNativeScrollable = isNodeScrollable(children);
const Container = useScrollContainer ? KeyboardAwareScrollView : View;

// 自带滚动容器时，仅包裹滚动组件
wrapScrollableWithKeyboardAvoid(children)
```

### 3.3 GPS 打卡功能（checkin-action）

**完整流程**：
```
进入页面 → 请求定位权限 → 获取 GPS 坐标 → Haversine 公式计算距离 → 200米内可打卡 → 后端二次验证 → 生成分享海报
```

**① 定位获取**
```ts
const { status } = await Location.requestForegroundPermissionsAsync();
const location = await Location.getCurrentPositionAsync({});
```

**② 前端距离验算（Haversine）**
```ts
const R = 6371000; // 地球半径（米）
const dLat = (lat2 - lat1) * Math.PI / 180;
const dLng = (lng2 - lng1) * Math.PI / 180;
const a = Math.sin(dLat/2)**2 + 
          Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * 
          Math.sin(dLng/2)**2;
const distance = R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
```

**③ 演示模式降级**
- 权限被拒绝、定位失败或超时时，自动进入 `demoMode`，前端强制 `success: true`，按钮变为"演示打卡"。

**④ 后端二次验证**
```ts
fetch(`${BASE_URL}/api/v1/checkin`, {
  method: 'POST',
  body: JSON.stringify({ user_id, attraction_id, lat, lng })
})
```
后端同样使用 Haversine 公式验算，防止前端伪造。

**⑤ 打卡成功分享海报**
- 使用 `react-native-view-shot` 的 `captureRef` 将 `ViewShot` 包裹的海报 DOM 截图为 PNG。
- 优先调用 `expo-sharing` 系统分享；不支持分享时降级到 `expo-clipboard` 复制文本。

**⑥ 导航跳转**
- 根据平台调用高德 App (`amap://route?dlat=...`) 或 Apple Maps / 高德 Web 导航。

### 3.4 AI 导游对话（chat）

**前端实现**：
- 消息状态用 `useState` 管理数组。
- 请求后端 `/api/v1/chat`，HTTP 错误或网络异常时降级到前端 `getFallbackResponse()`。
- 初始消息数 ≤ 2 时，底部展示横向快捷问题条。

**后端实现（双通道）**：
```ts
const cozeToken = process.env.COZE_API_TOKEN;
const cozeBotId = process.env.COZE_BOT_ID;

if (cozeToken && cozeBotId) {
  // 通道1：调用扣子 Coze API
  const response = await fetch('https://api.coze.cn/open_api/v2/chat', { ... });
  const data = await response.json();
  const reply = data.messages.find(m => m.role === 'assistant' && m.type === 'answer');
} else {
  // 通道2：本地预设回复
  generateFallbackResponse(message);
}
```
- Coze API 调用被 `try/catch` 包裹，异常时静默降级到本地回复。

### 3.5 成就徽章系统（badges）

**数据模型**：静态数组 `BADGES`，包含 `progress`、`isUnlocked`、`color`。

**渲染逻辑**：
- **已解锁**：彩色图标背景 + "已解锁"标签。
- **未解锁**：灰色图标 + 右下角锁覆盖层（`lockOverlay`）+ 底部百分比进度条。
- **Tab 过滤**：`selectedTab === 'all' ? BADGES : BADGES.filter(b => b.isUnlocked)`

### 3.6 主题与样式系统

**技术栈**：`tailwindcss` + `uniwind`

**变量定义**（`global.css`）：
- 亮色/暗色两套变量，通过 `@variant light/dark` 切换。
- 核心语义化 token：`--background`, `--foreground`, `--accent`, `--surface`, `--muted`, `--border`。
- 派生色通过 `color-mix(in oklab, ...)` 计算，如 `accent-hover`, `surface-secondary`。

**运行时读取**：
`uniwind` 的 `useCSSVariable` Hook 读取 DOM CSS 变量值，返回给 React Native 的 `StyleSheet` 使用，实现 Tailwind 主题与 RN 组件的桥接。

---

## 四、优化建议

### 4.1 数据层重构（最高优先级）

**问题**：所有数据都是静态/mock，且多处重复定义（景点数据在 `home`、`checkin`、`checkin-action`、`server` 中各有一份）。

**建议**：
1. **建立统一数据源**：
   - 前端新建 `api/` 目录，封装 axios/fetch 客户端 + 拦截器。
   - 新建 `types/` 目录，定义全局共享接口（`Attraction`, `Checkin`, `Badge`）。
2. **后端接入真实数据库**：
   - `server/package.json` 已引入 `drizzle-orm` + `pg`，建议立即启用。
   - 定义 `attractions`、`checkins`、`users` 等表结构。
3. **前端引入 TanStack Query**：
   - 替代裸 `useState` + `fetch`，自动处理缓存、重试、失效管理。

### 4.2 前端核心优化

| 优化项 | 当前问题 | 建议方案 |
|--------|----------|----------|
| 统一 API 客户端 | 每个页面重复写 `BASE_URL` + 裸 `fetch` | 新建 `api/client.ts`，统一 baseURL、超时、错误处理 |
| 图片优化 | 远程 URL 无缓存，无加载状态 | 使用 `expo-image` 的 `cachePolicy="memory-disk"`，添加骨架屏 |
| 列表性能 | FlatList 未优化渲染参数 | 添加 `initialNumToRender`、`maxToRenderPerBatch`、`removeClippedSubviews` |
| 减少重复定义 | `ATTRACTIONS_DATA` 分散在 4 个文件 | 提取到 `constants/attractions.ts`，API 统一获取 |
| 深色模式 | Profile 页 Switch 只改 state 不生效 | 连接 Switch 到 `ColorSchemeUpdater` 的 `setColorScheme` |

### 4.3 后端架构优化

1. **输入校验**：`zod` 已安装但未使用，建议给所有接口加上 `zod.safeParse`。  
   `/checkin` 的 `lat/lng` 应校验范围，`user_id` 应校验非空。

2. **路由模块化**：当前所有路由写在单个 `index.ts`，应拆分为 `routes/attractions.ts`、`routes/checkin.ts`、`routes/chat.ts` 等。

3. **全局错误处理中间件**：
   ```ts
   app.use((err, req, res, next) => {
     console.error(err.stack);
     res.status(500).json({ 
       error: process.env.NODE_ENV === 'production' ? 'Internal Server Error' : err.message 
     });
   });
   ```

4. **AI 对话流式输出**：当前阻塞式等待完整响应，建议支持 SSE (Server-Sent Events) 流式传输。

### 4.4 用户体验优化

1. **骨架屏替代 Loading 空白**：首页和打卡页数据加载时显示骨架屏。
2. **地图集成**：打卡功能只有距离数字，建议集成 `react-native-maps`，显示用户位置和打卡半径圆圈。
3. **离线能力**：景点列表本地持久化（`AsyncStorage`），打卡记录本地先写后同步。
4. **推送通知**：打卡成功、成就解锁时发送本地通知（`expo-notifications`）。

### 4.5 工程化与代码质量

1. **类型安全**：
   - 消除 `Ionicons name={icon as any}`，定义严格的图标名联合类型。
   - 前后端共用 `zod` schema 推导类型。

2. **组件抽取**：
   - `checkin-action/index.tsx` 目前 849 行，应抽取为：
     - `LocationStatus.tsx`
     - `CheckinPoster.tsx`
     - `ShareModal.tsx`

3. **样式统一**：
   - 大量页面使用硬编码色值（`'#6C63FF'`, `'#00B894'`），应全部收敛到 `global.css` 的 CSS 变量。

4. **测试**：
   - 后端 API 测试（`supertest`）
   - Haversine 距离计算单元测试
   - 核心组件快照测试

---

## 五、推荐实施路线图

| 阶段 | 内容 | 预期收益 |
|------|------|----------|
| **P0** | 统一 API 客户端 + 抽离类型定义 + Zod 校验 | 消除重复代码，提升稳定性 |
| **P0** | 接入 Drizzle ORM + PostgreSQL | 数据持久化，支持多用户 |
| **P1** | 引入 TanStack Query 替代裸 fetch | 缓存、重试、loading 状态统一管理 |
| **P1** | 后端路由模块化 + 错误中间件 | 可维护性大幅提升 |
| **P2** | 地图集成 + 流式 AI 输出 | 核心用户体验质变 |
| **P2** | 骨架屏 + 图片缓存优化 | 感知性能提升 |
| **P3** | 离线能力 + 推送通知 | 向生产级应用靠拢 |

### 最速见效的改动（1 小时内可完成）

1. **抽离 API 基础配置**：新建 `api/client.ts`，把所有页面的裸 `fetch` 换成封装后的调用。
2. **统一景点常量**：把分散在 4 个文件里的 `ATTRACTIONS` 合并到 `constants/attractions.ts`。
3. **后端加 Zod 校验**：给 `/checkin` 和 `/chat` 加上输入校验，避免崩溃。
4. **修复深色模式 Switch**：把 Profile 页的 Switch 实际绑定到主题切换逻辑。
